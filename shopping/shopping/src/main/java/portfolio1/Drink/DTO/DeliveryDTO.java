package portfolio1.Drink.DTO;

/*
import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.NoArgsConstructor;
import portfolio1.drink.DrinkEntity.DeliveryEntity;

@Data
@AllArgsConstructor
@NoArgsConstructor
@Builder
public class DeliveryDTO
{
    private Long idx; // 기본키
    private Long midx; // 회원 기본키
    private String name; // 받는분
    private String hp; // 연락처
    private String address; // 주소지
    private String request; // 요청사항
    private String regdate; // 주문일

    public DeliveryEntity toEntity()
    {
        return DeliveryEntity.builder()
                .idx(idx)
                .midx(midx)
                .name(name)
                .hp(hp)
                .address(address)
                .request(request)
                .regdate(regdate)
                .build();
    }
}
*/